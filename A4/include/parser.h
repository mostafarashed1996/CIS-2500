#ifndef _mrashed_PARSERH
#define _mrashed_PARSERH

#define MAXLINELENGTH	100

/*Food * parser(char * argv[])
Takes an array that contains the file name. This parses
the text file and parse it into different objects and returns
the head object that links everything else.
*/
Food * parser(char * argv[]);

#endif